export interface PlatosxPedido {
    id:number,
    plato: string,
    cantidad: number,
    nota: string,
}
export interface Cliente {
    id: number,
    nombre: string,
    direccion: string,
    telefono: string,
}